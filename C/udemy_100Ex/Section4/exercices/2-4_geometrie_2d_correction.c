#include <stdio.h>#include <stdlib.h>int main(){    int hauteur, largeur;    int perimetre, surface;    // Affichage de l'etat initial    printf("Donnez la hauteur et la largeur du rectangle:");    scanf("%d %d", &hauteur, &largeur);    //Calcules du perimetre du rectangle    perimetre = 2*hauteur + 2*largeur;    //Calcules de la surface du rectangle    surface = hauteur*largeur;    // Affichage du resultat    printf("\tPerimetre = %d\n", perimetre);    printf("\tSurface = %d\n", surface);    return 0;}