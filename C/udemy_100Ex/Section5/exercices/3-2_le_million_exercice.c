#include <stdio.h>
#include <stdlib.h>

int main(int argc, char const *argv[])
{
    int gain_user;

    printf("Combien avez-vous gagne?");
    scanf("%d", &gain_user);


    if (gain_user >= 1000)
    {
        print("%d" gain_user);
    }
    else if (gain_user >= 100)
    {
        /* code */
    }
    else
    {
        /* code */
    }



    return 0;
}
